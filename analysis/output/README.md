# Output status

All result files in this directory dated before the 2026-07-20 MoA remediation
are **legacy, stale, and not citable**. They were produced before the corrected
kill/assist model, canonical cohort, audited splits, run manifests, and revised
research constructs.

The following files were regenerated from the corrected pipeline on
2026-07-21 and are the current result set. Tabular/report outputs have a
matching `*.run.json` provenance manifest:

- mastery: `mastery_learning.{csv,json,md}`;
- ban salience: `ban_salience_by_tier.csv`, `design_ban_salience.{json,md}`;
- figures: regenerated by `analysis/make_figures.py` from those current files,
  with provenance recorded in `figures/figures.run.json`.

The current learning result is a descriptive negative finding, not evidence
that players causally become worse with experience. See
`docs/REMEDIATION_STATUS.md` before citing any coefficient or ranking.

The old `design_p*`, `perceived_gap*`, `skill_amplification*`, `matchup_*`, and
figure files are retained only to document the audit trail.
